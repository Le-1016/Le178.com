# Template only. Never commit real passwords or API keys.
DATABASE_URL=postgresql://lab_user:lab_password@postgres:5432/lab_db
PGHOST=postgres
PGPORT=5432
PGDATABASE=lab_db
PGUSER=lab_user
PGPASSWORD=lab_password
